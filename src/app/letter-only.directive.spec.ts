import { LetterOnlyDirective } from './letter-only.directive';

describe('LetterOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new LetterOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
